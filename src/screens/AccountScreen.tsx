const AccountScreen = () => {
  return <></>;
};

export default AccountScreen;

/**
 * Implement the screen that renders list of items
 *  - Use the ListItem component
 *  - Use the data from /data/AccountListItem.ts
 *  - Clicking on each item should take user to the specified url in the data
 *  - You can see the design in AccountScreenDesign.png
 *
 * Note: This task isn't testing your styling skills, so don't worry about styling
 */
